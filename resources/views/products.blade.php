@extends('layouts.app') @section('title','Corporate Gift Catalogue | Mugdi Investments LLC') @section('content')<section class="catalogue"><p class="eyebrow">CORPORATE GIFTS · LIVE AVAILABILITY</p><div class="section-head"><h1>Gifts worth<br>remembering.</h1><p>Explore current supplier availability. Our team will help with selection, branding, quantities and delivery.</p></div><form class="filters"><input type="search" name="q" value="{{ $search }}" placeholder="Search products"><select name="category"><option value="">All categories</option>@foreach($categories as $item)<option @selected($category===$item)>{{ $item }}</option>@endforeach</select><button class="button primary">Search</button></form>@if(!empty($catalogueError))<div class="notice"><h2>Catalogue connection in progress</h2><p>Email <a href="mailto:sales2@mugdiinvestments.com">sales2@mugdiinvestments.com</a> for options.</p></div>@elseif($products->isEmpty())<div class="notice"><h2>No matching gifts found</h2><p>Try a broader search or ask our team for a custom selection.</p></div>@else<div class="products">@foreach($products as $product)<article><div class="product-img"><img loading="lazy" src="{{ $product['image'] }}" alt="{{ $product['name'] }}"></div><div class="product-body"><div class="stock"><i></i>{{ number_format($product['quantity']) }} available</div><h2>{{ $product['name'] }}</h2><p>{{ $product['description'] }}</p><a href="{{ route('home') }}#contact" data-product="{{ $product['name'] }}">Enquire about this gift →</a></div></article>@endforeach</div>@endif</section>@endsection
